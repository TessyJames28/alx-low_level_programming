#include "main.h"

/**
 * _strlen - function to return length of string
 * @s: string variable whose length is returned
 * Return: Always 0
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
