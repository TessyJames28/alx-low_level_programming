#include "main.h"

/**
 * _abs - main function to check absolute value
 * @n: prototype type
 * Return: Always 0
 */

int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
	return (0);
}
