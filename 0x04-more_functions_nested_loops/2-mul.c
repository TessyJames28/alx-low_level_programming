#include "main.h"

/**
 * mul - function to multiply 2 integers
 * @a: first value for function parameter
 * @b: second value for function parameter
 * Return: Always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
